---
name: No gh CLI
description: Never use the gh CLI tool — user does not have it authenticated and does not want it used
type: feedback
---

Never use the `gh` CLI tool for any purpose. The user does not use it and does not want it invoked.

**Why:** User explicitly stated "We dont need gh and should never use gh."

**How to apply:** For GitHub-related tasks, use git commands directly or the browser. Do not run `gh` commands for checking runs, creating PRs, viewing issues, etc.
